<!-- scope: all | author: framework | regeneration: automatic | sources: user-zone vault notes -->
# STYLE.md

> **AUTO-GENERATED** by the harness distillation pass. Edit freely to seed or correct;
> the next distillation run will merge your edits against fresh observations rather than
> overwrite them blindly. **Source of truth: your own writing in the vault.**
> Last regenerated: _(never — seed pass pending)_

_This file captures the user's writing style so the agent can recognize it, echo it when appropriate, and avoid writing in a voice the user would find foreign. The distillation job samples user-zone notes (not agent-authored files — that would create a degenerate feedback loop), extracts style features, and rewrites this file. The user can edit any observation to correct it._

## What lives here (eventually)

- **Sentence length distribution** — average, median, range.
- **Punctuation habits** — em-dashes vs parens, Oxford comma, semicolon usage, exclamation density, lowercase-only habits.
- **Vocabulary quirks** — words they reach for, words they avoid, slang, idioms, running jokes.
- **Tag and heading conventions** — `#type/` prefixes, heading depth preferences, capitalization style.
- **Linking density** — how many wiki-links per note, backlink habits.
- **Rhythm** — lists vs prose, when they use callouts, paragraph length.
- **CAPS, bold, italic usage** — what they use for emphasis.
- **Typos they don't correct** — their personal dialect.

## Seed observations

<!-- PLACEHOLDER: empty until the first distillation pass runs. Seed manually if you want — the distiller will merge your seed with observed data. -->

_(This file is intentionally empty on first scaffold. Once the harness runtime lands and the distillation job runs, it will populate this section with concrete observations. Until then, `USER.md#how-they-talk` is the manual version of this file.)_
