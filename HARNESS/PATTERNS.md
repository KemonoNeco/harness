<!-- scope: all | author: framework | regeneration: automatic | sources: vault folder structure + link graph -->
# PATTERNS.md

> **AUTO-GENERATED** by the harness distillation pass. Edit freely to seed or correct;
> the next distillation run will merge your edits against fresh observations rather than
> overwrite them blindly. **Source of truth: your vault's folder structure and link graph.**
> Last regenerated: _(never — seed pass pending)_

_This file captures the user's PKM (personal knowledge management) patterns so the agent's own note-creation behavior in shared zones matches the user's conventions. If the user files notes before writing, the agent does too. If tags stabilize into a taxonomy, the agent respects it. The distillation job indexes folder structure and link patterns from user-zone notes and rewrites this file._

## What lives here (eventually)

- **Folder structure preferences** — flat vs nested, by-topic vs by-date, separate folders per project.
- **Linking style** — hubs-and-spokes, chains, bidirectional, orphan-friendly.
- **Filing habits** — file before writing, write before filing, inbox-first, inbox-free.
- **Tag taxonomy** — stable `#type/` prefixes, flat tags, nested tags, tag density per note.
- **Daily-note habits** — whether they use them, structure, how they promote to permanent notes.
- **Canvas / whiteboard usage** — when they reach for visual tools.
- **Template patterns** — recurring note structures (meeting notes, book notes, project logs).
- **Archival habits** — whether they delete, archive, or let notes accumulate.

## Seed observations

<!-- PLACEHOLDER: empty until the first distillation pass runs. Seed manually if you want — the distiller will merge your seed with observed data. -->

_(This file is intentionally empty on first scaffold. Once the harness runtime lands and the distillation job indexes the vault, it will populate this section with concrete observations.)_
